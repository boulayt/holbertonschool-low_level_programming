#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * _strlen - return length of a string
 * @s: input
 * Description: same
 * Return: strlen
 */
int _strlen(char *s)
{
	return (strlen(s));
}
