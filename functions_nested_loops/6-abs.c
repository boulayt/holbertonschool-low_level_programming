#include "main.h"

/**
 * print_sign - computes the absolute value of an integer
 * @n: number tested
 *
 * Description: same
 * Return: the absolute value of n 
 */
int _abs(int n)
{
	if (n < 0)
		return (n * (-1));
	return (0);
}
